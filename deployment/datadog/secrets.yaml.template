apiVersion: v1
kind: Secret
metadata:
  name: datadog-secret
type: Opaque
data:
  # Base64 encoded value - you need to replace this with your actual API key
  # To encode: echo -n "your-actual-api-key" | base64
  api-key: "REPLACE_WITH_YOUR_BASE64_ENCODED_API_KEY"
